zty
  (commands)
  - init
  - index
  - health

  - N, note
    (subcommands)
    - c, create
    - r, rename
    - rm, remove
    - m, merge
    (subflags)
    - -h, --help
    - -i, --interactive

  - C, compile
    (subflags)
    - -nc, --no-config
    - -l,  --linked
    - -t,  --tag
    - -h,  --help
    - -i,  --interactive

  - S, search
    (subflags)
    - -k, --keyword
    - -f, --file
    - -t, --tag
    - -l, --linked
    - -d, --date

  - T, tags
    (subcommands)
    - c,  create
    - a,  append
    - rm, remove
    (subflags)
    - --list
    - -h, --help

  (flags)
  - -h, --help
  - --working-dir=PATH
  - --no-input
